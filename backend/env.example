# Required: Google API Key for Gemini
GOOGLE_API_KEY=your_api_key

# Required: Shared secret for API authentication
CS_SHARED_SECRET=change_me

# Gemini model for analysis (default: gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash

# Maximum upload size in MB (default: 25)
MAX_UPLOAD_MB=25

# Temporary directory for file uploads (default: /data/tmp)
TMP_DIR=/data/tmp

# Logging level (default: INFO)
LOG_LEVEL=INFO

# Embedding model for semantic search (default: text-embedding-004)
EMBEDDING_MODEL=text-embedding-004

# Optional: Google Cloud Speech-to-Text configuration
# Uncomment and configure if using Google Cloud Speech-to-Text
# GOOGLE_APPLICATION_CREDENTIALS=/secrets/gcp-credentials.json
# STT_LANGUAGE_CODE=en-US
# STT_MODEL=default

# Optional: OpenAI Whisper configuration
# Uncomment and configure if using OpenAI Whisper for transcription
# OPENAI_API_KEY=your_openai_api_key
# WHISPER_MODEL=whisper-1

# Optional: Override Gemini model for STT (falls back to GEMINI_MODEL if not set)
# GEMINI_STT_MODEL=gemini-2.5-flash

# Image generation for memories (enabled via app settings)
# Uses Gemini 2.5 Flash Image (Nano Banana) model
# Generated when user enables "Generate images for memories" in app settings
# Cost: ~$0.03 per image (uses same GOOGLE_API_KEY)

